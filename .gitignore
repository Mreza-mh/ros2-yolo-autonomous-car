# .gitignore for smart_bot_ros2 (repo root)
# Generated: recommended ignores for ROS2 (colcon), Python, ML artifacts, IDEs and OS files.
# اگر می‌خواهید بعضی فولدرها (مثل `dataset/` یا فایل‌های وزن مدل) را نگه دارید، خط مربوطه را حذف یا با علامت ! سفید (negate) کنید.

# ----------------
# Colcon / ROS2
# ----------------
build/
install/
dataset/
images/
log/
build_*/
install_*/

# Local colcon command prefix env files
colcon_command_prefix_setup_py.sh
colcon_command_prefix_setup_py.sh.env

# Local setup scripts generated by install/build
local_setup.*
setup.*

# ----------------
# Python
# ----------------
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.pdb
*.egg-info/
dist/
build/
pip-wheel-metadata/
*.egg
*.whl

# Virtual environments
.venv/
venv/
env/
ENV/
venv*/

# Test / typecheck caches
.pytest_cache/
.mypy_cache/

# ----------------
# ML artifacts & datasets
# ----------------
# Large model weights and checkpoints (uncomment if you want to track some)
*.pt
*.pth
*.onnx
*.weights
*.tflite
*.ckpt

# Example explicit model file in repo (if already tracked, see instructions below)
yolov8n.pt

# Dataset - large image/label folders
dataset/
dataset/**
# If you want to keep dataset metadata but ignore images/labels, use this pattern instead:
# !dataset/dataset.yaml
# dataset/images/
# dataset/labels/

# ----------------
# IDEs / editors
# ----------------
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# ----------------
# OS and misc
# ----------------
.DS_Store
Thumbs.db
*.swp
*.tmp

# Logs & coverage
*.log
.coverage
coverage.xml

# Compiled / binary
*.so
*.o
*.out
